// import { expect, test } from '@playwright/test';

// test('Dashboard should display the main title', async ({ page }) => {
//   await page.goto('http://localhost:5173/admin/dashboard');
//   await expect(page.locator('text=Admin Dashboard')).toBeVisible();
// });

// test(' Dashboard should display Total Users count', async ({ page }) => {
//   await page.goto('http://localhost:5173/admin/dashboard');
//   await expect(page.locator('text=Total Users')).toBeVisible();
// });

// test(' Dashboard should display Users Pie Chart', async ({ page }) => {
//   await page.goto('http://localhost:5173/admin/dashboard');
//   await expect(page.locator('text=Users Pie Chart')).toBeVisible();
// });

// test(' Dashboard should display Total Products count', async ({ page }) => {
//   await page.goto('http://localhost:5173/admin/dashboard');
//   await expect(page.locator('text=Total Products')).toBeVisible();
// });

// test(' Dashboard should display Products Pie Chart', async ({ page }) => {
//   await page.goto('http://localhost:5173/admin/dashboard');
//   await expect(page.locator('text=Products Pie Chart')).toBeVisible();
// });
